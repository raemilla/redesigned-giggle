<%= react_component 'App',
  phase: ENV["phase"],
  pitches_per_student: ENV["pitches_per_student"],
  number_of_teams: ENV["number_of_teams"],
  size_of_pitch_subset: ENV["size_of_pitch_subset"],
  current_user: current_user.as_json,
  admin?: admin?,
  pitches: @pitches,
  students: @students
%>
